# huffmantree
This is a Huffman tree implementation in c++ that takes in a .txt file from the user and compresses it into binary bits then decompress the new compressed file. The program will ask for permission to see the user’s files, then takes a file path of a .txt file. Once it has the file it will compress it and output a new .txt file and create a decompressed version as well.
